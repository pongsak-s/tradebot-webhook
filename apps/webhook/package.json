{
  "name": "webhook",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest"
  },
  "dependencies": {
    "fastify": "^4.7.0",
    "@prisma/client": "*",
    "@monorepo/shared": "*",
    "zod": "^3.20.2",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "ts-node-dev": "^2.0.0",
    "vitest": "^0.23.0",
    "@types/node": "^18.7.18"
  }
}