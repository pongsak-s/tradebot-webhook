{
  "name": "webhook",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@monorepo/shared": "*",
    "@prisma/client": "*",
    "dotenv": "^10.0.0",
    "fastify": "^4.7.0",
    "zod": "^3.20.2"
  },
  "devDependencies": {
    "@types/node": "^18.7.18",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5",
    "vitest": "^0.23.0"
  }
}
